-- ============================================================
--  여러 날 데이트 지원: 장소(stops)에 날짜 컬럼 추가
--  Supabase SQL Editor 에서 한 번 실행.
-- ============================================================
alter table stops add column if not exists date date;
